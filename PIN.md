# Light Warrior PIN

## Current Focus

UE5 is the active production environment (Unreal Editor 5.8 at `D:\Games\UE_5.8`). C++ compiles pass, the procedural arena spawns a playable slice, and the automated UE iteration loop (`tools/ue-loop.ps1`) produces screenshot evidence. The design hook is: `Bravery expands the light.`

The Vite/Three.js prototype remains in the repo as a legacy reference but is no longer the active build target.

## Next Task

`design.reference_games`, then `playtest.first_10_seconds`

Both tasks should judge the build through the hook: safety, temptation, brave risk, power spike, and visible light expansion.

## Do Not Do Yet

- Do not add all four temples.
- Do not add bosses.
- Do not add progression before combat feel works.
- Do not mark UE5 tasks complete from the browser prototype.
